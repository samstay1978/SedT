<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX 包清单（目录打包方式）
  元素顺序严格遵循微软官方模板，makeappx 对顺序敏感，请勿调整。
  ★ Identity 的 Name / Publisher 必须与合作伙伴中心
    (Partner Center) 保留的应用标识完全一致，否则提交报
    "无效的软件包标识名称/发布者名称"：
      Name      = SamLi.Office
      Publisher = CN=DF81B408-E5B2-48DC-A919-4D0ABCEED8B5
    对应包系列名(PFN) = SamLi.Office_rmvsnw9t0fap4。
    Publisher 是账号发布者 ID（GUID 形式），不是显示名；
    商店提交无需自签名（商店自动重签）。侧载自签名证书的
    Subject 也必须与上面 Publisher 完全一致。
  包名 Name 只能包含字母/数字/点/连字符，3-50 字符，不能有空格。
-->
<Package xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
         xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
         xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
         IgnorableNamespaces="uap rescap">

  <!-- 身份：包名 / 发布者ID（=合作伙伴中心预留值）/ 版本(Major.Minor.Build.Revision) -->
  <Identity Name="SamLi.Office"
            Publisher="CN=DF81B408-E5B2-48DC-A919-4D0ABCEED8B5"
            Version="2.4.0.0" />

  <Properties>
    <DisplayName>Office敏感词加解密工具</DisplayName>
    <PublisherDisplayName>Sam Li</PublisherDisplayName>
    <Description>Office 文档（Word/Excel/PPT）敏感词替换加密工具</Description>
    <Logo>Assets\StoreLogo.png</Logo>
  </Properties>

  <Resources>
    <Resource Language="zh-CN" />
  </Resources>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <!-- 桌面（全信任）应用能力：exe 正常运行必需 -->
  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>

  <Applications>
    <!-- 入口 exe：相对包根目录；EntryPoint 固定为 Windows.FullTrustApplication -->
    <Application Id="OfficeSensitiveEncryptor"
                 Executable="OfficeSensitiveEncryptor.exe"
                 EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements DisplayName="Office敏感词加解密工具"
                          Square150x150Logo="Assets\Square150x150Logo.png"
                          Square44x44Logo="Assets\Square44x44Logo.png"
                          Description="Office 文档（Word/Excel/PPT）敏感词替换加密工具"
                          BackgroundColor="transparent" />
      <!-- 桌面快捷方式说明：MSIX 下 exe 本体位于 WindowsApps 受限目录，
           普通用户进程不能直接执行该路径；程序首启自动创建的桌面快捷
           方式经 explorer.exe 打开 shell:AppsFolder\<包系列名>!<应用ID>
           （系统应用模型激活）启动，双击可正常打开，升级后始终有效。
           注意：不要在此声明 uap3 AppExecutionAlias 执行别名扩展——
           部分 Windows SDK 版本的 makeappx 对该扩展的子元素校验存在
           兼容性问题（报 C00CE014），会导致打包失败。 -->
    </Application>
  </Applications>

</Package>
